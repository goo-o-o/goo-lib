package com.goo.goo_lib.utils.text.effect.config.base;

public interface EffectConfig {
    // Could add later if needed:
    // default boolean affectsVertices() { return false; }
    // default boolean needsOverlayPass() { return false; }
}